- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.bash_aliases:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
