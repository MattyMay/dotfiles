- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.bash_aliases:
    ~/.bash_prompt:
    ~/.inputrc:
    ~/.ideavimrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
